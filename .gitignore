# =========================================================================
# .NET (Backend/)
# =========================================================================
[Bb]in/
[Oo]bj/
[Pp]ublish/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
[Tt]est[Rr]esult*/
*.dbmdl
*.pdb

# =========================================================================
# Node / Vite (Frontend/warehouse-client, Frontend/TestOrderGenerator)
# =========================================================================
node_modules/
dist/
dist-ssr/
build/
.vite/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Playwright (Frontend/warehouse-client/e2e)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# =========================================================================
# Docker — local-only overrides (e.g. remapping a port that conflicts with
# something already running on a dev machine). Never meant to reach a commit;
# `docker compose` picks this up automatically, no -f flag needed.
# =========================================================================
docker-compose.override.yml

# =========================================================================
# Environment / secrets
# =========================================================================
.env
.env.local
.env.*.local
# Keep the documented template committed.
!.env.example

# =========================================================================
# Editors / OS
# =========================================================================
.vscode/
.idea/
*.DotSettings.user
.DS_Store
Thumbs.db
Desktop.ini

# =========================================================================
# Tool-generated, not part of the shipped app — never meant to be visible
# in the repo (config/cache/analysis artifacts, not source or docs).
# =========================================================================
.claude/
**/graphify-out/
